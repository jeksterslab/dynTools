% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynTools-regularize-time-by-id.R
\name{RegularizeTimeByID}
\alias{RegularizeTimeByID}
\title{Regularize Time by ID}
\usage{
RegularizeTimeByID(
  data,
  id,
  time,
  observed,
  covariates = NULL,
  delta_t,
  grid = c("global", "by_id"),
  method = c("preserve", "snap")
)
}
\arguments{
\item{data}{Data frame.
A data frame object of data for potentially
multiple subjects that contain
a column of subject ID numbers
(i.e., an ID variable),
a column indicating subject-specific measurement occasions
(i.e., a TIME variable),
at least one column of observed values.}

\item{id}{Character string.
A character string of the name of the ID variable in the data.}

\item{time}{Character string.
A character string of the name of the TIME variable in the data.}

\item{observed}{Character vector.
A vector of character strings
of the names of the observed variables in the data.}

\item{covariates}{Character vector.
A vector of character strings
of the names of the covariates in the data.}

\item{delta_t}{Positive number.
Time interval.}

\item{grid}{Character string.
If \code{grid = "global"}, use one time grid from the global minimum to the
global maximum time value. If \code{grid = "by_id"}, use an ID-specific time
grid from each ID's minimum to maximum time value.}

\item{method}{Character string.
If \code{method = "preserve"}, complete the regular grid but preserve observed
off-grid time values. If \code{method = "snap"}, snap observed time values to
the nearest grid point so the output contains only regular grid times.
When multiple rows for the same ID snap to the same grid point, the row
closest to the grid point is kept; ties are broken by the largest number
of non-missing observed/covariate values and then by original order.}
}
\value{
Returns a data frame.
}
\description{
The function inserts rows with missing values so that time follows a regular
grid. A global grid uses the same time sequence for every ID. An ID-specific
grid uses each ID's own minimum and maximum observed time values.
If \code{method = "preserve"}, the function completes the regular grid while
retaining empirical off-grid time values. If \code{method = "snap"}, empirical
time values are snapped to the nearest grid point and the returned data use
only grid time values.
}
\examples{
data <- data.frame(
  id = c(1, 1, 2, 2),
  time = c(1, 3, 1, 2),
  y = c(10, 12, 20, 21)
)
data

RegularizeTimeByID(
  data = data,
  id = "id",
  time = "time",
  observed = "y",
  delta_t = 1,
  grid = "by_id"
)

RegularizeTimeByID(
  data = data.frame(
    id = c(1, 1),
    time = c(1.0, 1.7),
    y = c(10, 17)
  ),
  id = "id",
  time = "time",
  observed = "y",
  delta_t = 0.5,
  grid = "by_id",
  method = "snap"
)

}
\seealso{
Other Dynamic Modeling Utility Functions:
\code{\link[=CheckDynData]{CheckDynData()}},
\code{\link[=DeleteInitialNA]{DeleteInitialNA()}},
\code{\link[=DeltaTByID]{DeltaTByID()}},
\code{\link[=DetrendByID]{DetrendByID()}},
\code{\link[=ElapsedTimeByID]{ElapsedTimeByID()}},
\code{\link[=FilterByID]{FilterByID()}},
\code{\link[=InitialNA]{InitialNA()}},
\code{\link[=InsertNA]{InsertNA()}},
\code{\link[=LagByID]{LagByID()}},
\code{\link[=MakeClockTime]{MakeClockTime()}},
\code{\link[=PlotByID]{PlotByID()}},
\code{\link[=ReplaceMissingCode]{ReplaceMissingCode()}},
\code{\link[=ResolveDuplicateIDTime]{ResolveDuplicateIDTime()}},
\code{\link[=RoundClockTime]{RoundClockTime()}},
\code{\link[=ScaleByID]{ScaleByID()}},
\code{\link[=SubsetByID]{SubsetByID()}},
\code{\link[=SummaryByID]{SummaryByID()}}
}
\author{
Ivan Jacob Agaloos Pesigan
}
\concept{Dynamic Modeling Utility Functions}
\keyword{data}
\keyword{dynTools}
